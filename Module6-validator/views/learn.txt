 here we have to install the package for the checking and filtering the data
=>validator 
    1. npm install express-validator or sudo npm install express-validator
    2.const { check, validationResult } = require('express-validator');
        // this is used for validation in the express Js
